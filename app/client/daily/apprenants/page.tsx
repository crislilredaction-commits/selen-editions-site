"use client";
import Link from "next/link";
import {FormEvent,useCallback,useEffect,useMemo,useState} from "react";
import {assistanceFetch} from "@/components/AgentAssistanceBanner";
import LoadingMascot from "@/components/ui/LoadingMascot";
type Learner={id:string;first_name:string;last_name:string;email?:string|null;phone?:string|null;company_name?:string|null;job_title?:string|null;status:string};type Session={id:string;internal_reference?:string|null;start_date?:string|null;end_date?:string|null;daily_formations?:{title?:string|null}|null};type Enrolment={id:string;session_id:string;learner_id:string;status:string;funding_type:string;positioning_status:string;prerequisites_status:string;daily_learners?:Learner|null;daily_sessions?:Session|null};type Portal={id:string;enrolment_id:string;learner_id:string;token:string;status:string;expires_at?:string|null;last_viewed_at?:string|null};
const card:React.CSSProperties={border:"1px solid var(--sepia-mid)",background:"var(--paper)",padding:"1rem"};const field:React.CSSProperties={width:"100%",padding:".6rem",border:"1px solid var(--sepia-mid)",background:"white"};const compact:React.CSSProperties={width:"fit-content",padding:".55rem .75rem",border:"1px solid var(--rust)",background:"var(--rust)",color:"white",fontWeight:800,cursor:"pointer"};
export default function Page(){const[learners,setLearners]=useState<Learner[]>([]);const[enrolments,setEnrolments]=useState<Enrolment[]>([]);const[portal,setPortal]=useState<Portal[]>([]);const[sessions,setSessions]=useState<Session[]>([]);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[error,setError]=useState("");const[message,setMessage]=useState("");const[showCreate,setShowCreate]=useState(false);const[open,setOpen]=useState<string|null>(null);const[learnerId,setLearnerId]=useState("");const[sessionId,setSessionId]=useState("");const[lastEnrolment,setLastEnrolment]=useState<{id:string;learnerId:string;sessionId:string}|null>(null);
const load=useCallback(async()=>{try{const[a,b]=await Promise.all([assistanceFetch("/api/client/daily/learners",{cache:"no-store"}),assistanceFetch("/api/client/daily/sessions",{cache:"no-store"})]);const ad=await a.json().catch(()=>({}));const bd=await b.json().catch(()=>({}));if(!a.ok)throw new Error(ad.error??"Impossible de charger les apprenants.");if(!b.ok)throw new Error(bd.error??"Impossible de charger les sessions.");setLearners(ad.learners??[]);setEnrolments(ad.enrolments??[]);setPortal(ad.portalAccess??[]);setSessions((bd.sessions??[]).filter((x:{status?:string})=>x.status!=="archived"))}catch(e){setError(e instanceof Error?e.message:"Chargement impossible.")}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);const portalBy=useMemo(()=>new Map(portal.map(x=>[x.enrolment_id,x])),[portal]);
async function createLearner(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);const f=new FormData(e.currentTarget);const r=await assistanceFetch("/api/client/daily/learners",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"learner",first_name:f.get("first_name"),last_name:f.get("last_name"),email:f.get("email"),phone:f.get("phone"),company_name:f.get("company_name"),job_title:f.get("job_title")})});const d=await r.json().catch(()=>({}));setSaving(false);if(!r.ok)return setError(d.error??"Création impossible.");e.currentTarget.reset();setShowCreate(false);setMessage("Apprenant ajouté.");await load()}
async function enrol(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setLastEnrolment(null);const f=new FormData(e.currentTarget);const r=await assistanceFetch("/api/client/daily/learners",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"enrolment",learner_id:learnerId,session_id:sessionId,funding_type:f.get("funding_type"),funding_organisation:f.get("funding_organisation"),company_name:f.get("company_name"),company_contact_name:f.get("company_contact_name"),company_contact_email:f.get("company_contact_email")})});const d=await r.json().catch(()=>({}));setSaving(false);if(!r.ok)return setError(d.error??"Inscription impossible.");const created=d.enrolment?.id||d.id||"";setLastEnrolment({id:created,learnerId,sessionId});setMessage("Inscription créée. Vous pouvez maintenant préparer et envoyer le dossier d'inscription.");await load()}
if(loading)return <LoadingMascot message="Sélion rassemble les apprenants…"/>;return <main style={{maxWidth:1120,margin:"0 auto",padding:"2rem 1rem 5rem",display:"grid",gap:16}}><header><p className="gazette-label">Selen Daily · Apprenants</p><h1>Apprenants & inscriptions</h1><p>Chaque apprenant conserve un dossier unique et l'historique de ses inscriptions.</p></header>{error?<p style={{color:"#a33"}}>{error}</p>:null}{message?<p style={{border:"1px solid #668153",padding:10,color:"#455a3b"}}>{message}</p>:null}
<section style={card}><button type="button" onClick={()=>setShowCreate(v=>!v)} style={{width:"100%",display:"flex",justifyContent:"space-between",border:0,background:"transparent",fontWeight:800,fontSize:17,cursor:"pointer"}}><span>Créer un nouvel apprenant</span><span>{showCreate?"▲":"▼"}</span></button>{showCreate?<form onSubmit={createLearner} style={{display:"grid",gap:10,marginTop:12}}><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(210px,1fr))",gap:8}}><input name="first_name" required placeholder="Prénom *" style={field}/><input name="last_name" required placeholder="Nom *" style={field}/><input name="email" type="email" placeholder="Email" style={field}/><input name="phone" placeholder="Téléphone" style={field}/><input name="company_name" placeholder="Entreprise" style={field}/><input name="job_title" placeholder="Fonction" style={field}/></div><button disabled={saving} style={compact}>Ajouter l'apprenant</button></form>:null}</section>
<form onSubmit={enrol} style={{...card,border:"2px solid var(--rust)"}}><p className="gazette-label">Inscription manuelle</p><h2>Créer l'inscription</h2><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(210px,1fr))",gap:8}}><select required value={learnerId} onChange={e=>setLearnerId(e.target.value)} style={field}><option value="">Choisir un apprenant</option>{learners.filter(x=>x.status==="active").map(x=><option key={x.id} value={x.id}>{x.first_name} {x.last_name}</option>)}</select><select required value={sessionId} onChange={e=>setSessionId(e.target.value)} style={field}><option value="">Choisir une session</option>{sessions.map(x=><option key={x.id} value={x.id}>{x.daily_formations?.title??"Session"} · {x.internal_reference??x.start_date??x.id.slice(0,8)}</option>)}</select><select name="funding_type" defaultValue="unknown" style={field}><option value="unknown">Financement à préciser</option><option value="employer">Employeur</option><option value="opco">OPCO</option><option value="public_funder">Financeur public</option><option value="self_funded">Autofinancement</option><option value="other">Autre</option></select><input name="funding_organisation" placeholder="Financeur" style={field}/><input name="company_name" placeholder="Entreprise commanditaire" style={field}/><input name="company_contact_name" placeholder="Contact entreprise" style={field}/><input name="company_contact_email" type="email" placeholder="Email du contact" style={field}/></div><button disabled={saving} style={{...compact,marginTop:10}}>Créer l'inscription</button>{lastEnrolment?<div style={{marginTop:12,padding:12,border:"1px solid var(--sepia-mid)",background:"#fffaf0"}}><strong>Étape suivante</strong><p style={{margin:"6px 0 10px"}}>Préparez le dossier d'inscription avec les informations de cet apprenant puis envoyez-le sans avoir à rechercher à nouveau son dossier.</p><Link href={`/client/daily/generateur-documents?learner=${encodeURIComponent(lastEnrolment.learnerId)}&session=${encodeURIComponent(lastEnrolment.sessionId)}&enrolment=${encodeURIComponent(lastEnrolment.id)}`} style={compact}>Préparer et envoyer le dossier d'inscription →</Link></div>:null}</form>
<section style={card}><h2>Répertoire des apprenants</h2><div style={{display:"grid",gap:8}}>{learners.map(l=>{const opened=open===l.id;const history=enrolments.filter(e=>e.learner_id===l.id);return <article key={l.id} style={{borderTop:"1px solid var(--sepia-mid)",paddingTop:8}}><button type="button" onClick={()=>setOpen(opened?null:l.id)} style={{width:"100%",display:"flex",justifyContent:"space-between",textAlign:"left",border:0,background:"transparent",cursor:"pointer",color:"inherit"}}><span><strong>{l.first_name} {l.last_name}</strong><small style={{display:"block",color:"var(--ink-soft)"}}>{l.email||"Email non renseigné"}{l.company_name?` · ${l.company_name}`:""}</small></span><span>{opened?"▲":"▼"}</span></button>{opened?<div style={{marginTop:10,padding:12,border:"1px solid var(--sepia-mid)",background:"#fffaf0",display:"grid",gap:12}}><div><h3>Coordonnées</h3><p>{l.email||"Email non renseigné"}{l.phone?` · ${l.phone}`:""}{l.job_title?` · ${l.job_title}`:""}</p></div><div><h3>Historique formations & sessions</h3>{history.length?<div style={{display:"grid",gap:8}}>{history.map(e=>{const p=portalBy.get(e.id);return <div key={e.id} style={{borderLeft:"3px solid var(--rust)",paddingLeft:10}}><strong>{e.daily_sessions?.daily_formations?.title??"Formation"}</strong><p style={{margin:"3px 0"}}>{e.daily_sessions?.internal_reference??"Session"} · {e.daily_sessions?.start_date??"Date à préciser"} · {e.status}</p><small>Positionnement : {e.positioning_status} · Prérequis : {e.prerequisites_status}{p?.last_viewed_at?` · Espace consulté le ${new Intl.DateTimeFormat("fr-FR").format(new Date(p.last_viewed_at))}`:""}</small></div>})}</div>:<p>Aucune inscription.</p>}</div><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><Link href={`/client/daily/generateur-documents?learner=${encodeURIComponent(l.id)}`} style={compact}>Documents du dossier</Link><span style={{padding:".55rem .75rem",border:"1px dashed var(--sepia-mid)",color:"var(--ink-soft)",fontSize:13}}>Notes horodatées et chronologie reçus/signés : raccord base de données requis</span></div></div>:null}</article>})}</div></section></main>}
